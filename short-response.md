# Short Response Questions
---

1 Me and my classmate would have to frist share the repository with-eachother. This process would happen in the settings of the repository and then the collaboraters tab, where then we exchange emails, or one teammate has to add another teammate. This is important because we both have to working of the same repository in order for things to go accordingly. 

2 Developers use branches so they can either, add, fix, or experiment something without disrupting or messing up the main repository.

3 Git is a open-source version control system. Git-hub is a cloud base hosting system. Developers that use Git through Github can have access to their entire codebase & history.

4 
```
git commit -m "saving work"
git add .
git push
```
This line of code is incorrect because the first step and second step should be swapped. The correct line of code is
```
git add .
git commit -m "saving work"
git push
```
